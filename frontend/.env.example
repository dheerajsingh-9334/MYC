# Copy to .env.local for development or to .env.production for deployment.
# Every variable exposed to the browser MUST be prefixed with NEXT_PUBLIC_.

# Base URL of the backend API (no trailing slash).
# - Dev:  http://localhost:4000
# - Prod: https://api.your-domain.com
NEXT_PUBLIC_API_URL=http://localhost:4000