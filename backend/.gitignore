# dependencies
/node_modules

# build output
/dist

# uploaded files (ephemeral on most PaaS; mount a volume or use S3 in prod)
/uploads

# typescript build cache
*.tsbuildinfo

# env files — ignore all by default, but keep the .env.example template committed
.env*
!.env.example

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor / OS
.DS_Store
.idea/
.vscode/
